# Amendment A003 Implementation Plan (Internal — Do Not Commit)
# Generated: 2026-06-29
# Source: dev-history/s2-006-infrastructure-correction-amendment.md

## Phase 0: Commit Amendment Document
[P0-1] Copy dev-history/s2-006-infrastructure-correction-amendment.md → experiments/study_002/pre-registration-amendment-003.md

## Phase 1: Clear Prior Artifacts
[P1-1] Delete from experiments/study_002/:
  REMOVE: metrics.jsonl, routing_history.jsonl, episodes.jsonl, assessments.jsonl,
    agent-rationale.jsonl, anomalies.jsonl, model_performance.jsonl, .perf_start_ts,
    all attention_scores_*.json, entire iterations/ directory
  KEEP: pre-registration.md, pre-registration-proof.txt, probe_set.json, pre-registration-amendment-003.md

## Phase 2: Restore Baseline Files
[P2-1] Write naive prompt (amendment lines 352-363) to prompts/system_prompt.md
[P2-2] Compute SHA-256 hash of restored prompt, fill into [PLACEHOLDER] in pre-registration-amendment-003.md
[P2-3] Empty prompts/examples.md (zero bytes)
[P2-4] Write naive extractor (amendment lines 369-391) to playground/extractor.py
[P2-5] Verify playground/ contains only __init__.py and extractor.py (no preprocessor.py, validator.py)
[P2-6] Clean playground/__pycache__/

## Phase 3: Attention Signal Redesign (A003-1)
[P3-1] protected/attention/scorer.py:
  - Extend RoutingScore dataclass: add score_start: float, score_end: float, intra_generation_delta: float
  - Update compute_routing_score(start_attn: Tensor, end_attn: Tensor, sentence_map) → return all three scores
  - score field = score_end for backward compat

[P3-2] protected/attention/analyzer.py:
  - Replace run_prefill with run_generation_attention (amendment lines 407-505):
    * Prefill: output_attentions=False, get KV cache
    * Decode loop (max_new_tokens=20, hardcoded): output_attentions=True
    * Capture start_attn_raw at step 0
    * Capture end_attn_raw at each step (last wins, or EOS breaks loop)
    * Return (start_attn, end_attn) 1D tensors
  - Add _extract_abstract_attn helper (averages last 6 layers, all heads, slices to abstract tokens)
  - Update analyze_abstract to use run_generation_attention, construct RoutingScore with all new fields
  - Deprecate extract_last_token_attention (kept, not exported)

[P3-3] protected/attention/__init__.py:
  - Remove run_prefill and extract_last_token_attention from exports
  - Add run_generation_attention

[P3-4] protected/attention/forward_pass_runner.py (lines 88-98):
  - Add score_start, score_end, intra_generation_delta to serialization dict

[P3-5] Smoke-test verify_attention_pipeline and verify_sensitivity_to_prompt_change

## Phase 4: Routing History + Reconstruction
[P4-1] protected/harness/study_002/routing_history.py:
  a) _serialize (lines 22-36): add score_start, score_end, intra_generation_delta to dict
  b) format_for_agent: new table format per amendment (lines 534-549):
     Abstract | Start | End | Intra Δ | Iter 0 | Iter 1 | ... | Current
     Include interpretation notes for Start/End/Intra Δ/Iter delta
  c) Keep _compute_delta_stats and format_routing_delta (use .score = score_end)

[P4-2] protected/harness/study_002/study_runner.py:
  - _dicts_to_routing_scores (lines 251-265): add score_start, score_end, intra_generation_delta
  - Use .get() with default 0.0 for backward compat with old routing_history data

## Phase 5: Smoke Test (A003-5)
[P5-1] protected/harness/shared/interface_validator.py:
  - Add step 5 to validate_interface(): async invoke extract() on test abstract:
    "Bilateral hippocampal activation was significantly increased during
     encoding compared to baseline (p < 0.001). Left prefrontal cortex showed
     greater activation for novel stimuli than repeated stimuli."
  - Use asyncio.run() wrapper (validator is sync context)
  - Valid: any ExtractionResult without raising (even zero claims)
  - On failure: log interface_smoke_test_failed anomaly
  - Extend ValidationResult: add smoke_test_passed: bool, smoke_test_claim_count: int

## Phase 6: Episode Schema (A003-4)
[P6-1] protected/harness/shared/edit_protocol.py:
  - Import field from dataclasses
  - Episode dataclass: add edits_applied: bool = False, field_failures: list[str] = field(default_factory=list)
  - Defaults ensure backward compat with all existing Episode construction sites

[P6-2] protected/harness/shared/episode_store.py:
  - Update append() serialization (lines 16-22): add edits_applied, field_failures to record dict
  - NO discard logic exists here (PM finding — discard is in study_runner.py)

## Phase 7: Field-by-Field Agent Calls (A003-2, A003-3)
[P7-1] protected/harness/study_002/agent_caller.py:
  - KEEP invoke_diagnostic() and invoke_decision() as orchestrators that internally
    call field-level functions. Preserves study_runner.py imports (line 40).
  - invoke_diagnostic() now calls 4 field functions, collects results, returns AssessmentResult
  - invoke_decision() now calls 5 field functions, collects results, returns AgentResponse

  Call 1 field functions:
    invoke_diagnostic_routing_trend(context) → "improving" | "declining" | "flat"
    invoke_diagnostic_last_action_effect(context) → plain text paragraph
    invoke_diagnostic_pattern_observed(context) → plain text paragraph
    invoke_diagnostic_hypothesis(context) → plain text paragraph

  Call 2 field functions:
    invoke_episode_observation(context) → plain text
    invoke_episode_hypothesis(context) → plain text
    invoke_episode_action(context) → plain text
    invoke_episode_expectation(context) → plain text
    invoke_edits(context) → raw JSON array

  Each field call:
    - Same system framing + context (prefill content), different completion prompt
    - Wrapped in try/except → on failure, log field_call_failed anomaly, use default
    - Defaults: text fields → "[not available — call failed]", routing_trend → "flat"
    - invoke_edits: only JSON call, failure triggers repair loop (3 attempts)

  Few-shot examples (A003-2):
    - 3 scenarios (amendment lines 127-160) embedded in _build_diagnostic_prompt system framing
    - Teach causal interpretation of start/end/intra signals

  New helper:
    _parse_edits_array(raw) → list[dict] — parses raw JSON array (no wrapping object)
    invoke_repair stays as-is

## Phase 8: Study Runner Integration (A003-4, A003-6, A003-7)
[P8-1] protected/harness/study_002/study_runner.py:

  a) Baseline verification (A003-7):
     New function _verify_baseline_state(study_id):
       1. SHA-256 of prompts/system_prompt.md == locked hash (from amendment)
       2. prompts/examples.md is empty (zero bytes or whitespace only)
       3. playground/ contains exactly __init__.py and extractor.py
       4. experiments/study_002/metrics.jsonl is empty or absent
       On failure: log baseline_state_invalid anomaly, raise RuntimeError

     Call from _pre_run_checks (after existing checks, before model load)

  b) Remove episode discard logic (lines 550, 664, 676):
     - Remove log_anomaly("episode_discarded") calls
     - Episodes always persisted regardless of edit outcome

  c) Context windowing (A003-6):
     In _run_iteration: pass prior_episodes[-5:] to invoke_decision (not all episodes)
     Call 1 (diagnostic) still gets full routing history

  d) Episode persistence (A003-4):
     - After field-by-field calls: construct Episode with edits_applied, field_failures
     - Persist episode BEFORE edit application
     - If edits succeed: edits_applied = True
     - If edits fail (repair exhausted): edits_applied = False, episode still persisted
     - field_failures = list of field names that returned defaults

  e) Smoke test integration:
     - Before interface validation loop, run smoke test
     - If smoke test fails: trigger repair loop (not corpus run)
     - Record smoke_test_passed, smoke_test_claim_count in metrics

  f) Metrics new fields in _make_metrics_base and _run_iteration:
     avg_routing_score_start, avg_routing_score_end, avg_intra_generation_delta
     edits_applied, field_failure_count, smoke_test_passed, smoke_test_claim_count

  g) Routing aggregation: compute avg of score_start, score_end, intra_generation_delta
     from pre_scores and post_scores (in addition to existing .score aggregation)

## Phase 9: Metrics + Artifacts
[P9-1] protected/harness/shared/artifact_writer.py:
  - append_metrics_study_002: handle new fields
  - Compute routing start/end/intra averages from scores passed in

## Phase 10: Anomaly Types
[P10-1] protected/harness/shared/anomaly_logger.py:
  - No code change (generic, accepts any anomaly_type string)
  - Document new types in comments:
    field_call_failed, interface_smoke_test_failed, baseline_state_invalid

## Execution Order
Phase 0 → Phase 1 → Phase 2 (housekeeping)
Phase 3 → Phase 4 → Phase 5 (attention + infra)
Phase 6 → Phase 7 → Phase 8 → Phase 9 → Phase 10 (agent caller + runner integration)

## Key Design Decisions
- max_new_tokens = 20 (hardcoded, per amendment)
- score field = score_end (backward compat)
- Episodes ALWAYS persisted (edits_applied boolean partitions data)
- Field-by-field calls share identical prefill (KV cache sharing NOT implemented — latency trade-off acknowledged)
- Edits call returns raw JSON array (no wrapping object)
- invoke_diagnostic/decision preserved as orchestrators (backward compat with study_runner imports)
- Episode dataclass fields have defaults (backward compat with existing construction)
- _dicts_to_routing_scores uses .get() with defaults (backward compat with old routing data)
